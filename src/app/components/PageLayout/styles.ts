import { getDashedText } from "../../utils/text.ts";
import { BACKGROUND_CLASS_NAMES } from "../../constants/siteStyles.ts";

export const defaultBackgroundClassName = getDashedText(
  BACKGROUND_CLASS_NAMES.LINED_PAPER,
);
